/*
Deployment script for AutoprevoznikDB

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "AutoprevoznikDB"
:setvar DefaultFilePrefix "AutoprevoznikDB"
:setvar DefaultDataPath "C:\Program Files\Microsoft SQL Server\MSSQL12.SQLEXPRESS\MSSQL\DATA\"
:setvar DefaultLogPath "C:\Program Files\Microsoft SQL Server\MSSQL12.SQLEXPRESS\MSSQL\DATA\"

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
PRINT N'Creating [dbo].[GetSeat]...';


GO
CREATE FUNCTION [dbo].[GetSeat]
(
	@reg int
)
RETURNS INT
AS
BEGIN
	declare @nextFreeSeat int = -1;
	declare @br_mesta int;
	declare @seatsBought int;

	set @br_mesta = (select br_mesta from Autobus where reg = @reg);

	if(@br_mesta is not null)
	begin
		set @seatsBought = (select bought = count(*) from Karta where Putuje_Autobus_reg = @reg group by Putuje_Autobus_reg,Putuje_Linija_br_linije,Putuje_dv_polaska);
		if(@seatsBought is not null)
		begin
			if(@seatsBought < @br_mesta)
				set @nextFreeSeat = @seatsBought + 1;
			else
				set @nextFreeSeat = 0;
			
		end
	end
	else
		set @nextFreeSeat = 0;

	RETURN @nextFreeSeat
END
GO
PRINT N'Update complete.';


GO
